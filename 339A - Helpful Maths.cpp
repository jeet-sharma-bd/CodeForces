
#include<bits/stdc++.h> 
using namespace std; 

int main() 
{ 
    string s;
    cin>>s;
    
    sort(str.begin(), str.end()); 
    	
    	for(int i=s.size()/2;i<s.size();i++){
    		cout<<s[i];
    		if(i+1 != s.size()){
    			cout<<"+";
			}
		}
    cout<<endl;
    return 0; 
} 
